#!/usr/bin/env bash

goVersion=1.17.6
goURL="https://go.dev/dl/go${goVersion}.linux-amd64.tar.gz"

# Check if Go is downloaded to the cache/ folder

# Check if Go is already installed

# Check if GoVersion installed is greater than the version we want to installed
